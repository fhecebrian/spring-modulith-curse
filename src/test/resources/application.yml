spring:
  datasource:
    username: ${MODULITH_USERNAME:modulitha}
    password: ${MODULITH_PASSWORD:modulitha}
    url: jdbc:postgresql://localhost:5432/spring_modulith

#  docker:
#    compose:
#      skip:
#        in-tests: false

logging:
  level:
    org.springframework.modulith: DEBUG